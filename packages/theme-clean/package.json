{
  "name": "@rebar-ui/theme-clean",
  "version": "0.11.0",
  "description": "Rebar's plain, production-safe baseline theme — the first step before a full custom re-skin.",
  "type": "module",
  "files": [
    "dist"
  ],
  "exports": {
    "./theme.css": "./dist/theme.css"
  },
  "scripts": {
    "build": "mkdir -p dist && cp src/theme.css dist/theme.css",
    "dev": "npm run build",
    "test": "echo \"no tests\"",
    "typecheck": "echo \"no typecheck\""
  },
  "license": "MIT"
}
